import { Button, styled } from '@mui/material';

/**
 * Customized Opencommerce button.
 */
const ColorButton = styled(Button)(() => ({
  borderRadius: '8px',
  textTransform: 'none',
  fontWeight: 'bold',
}));

export default ColorButton;
