mutation UploadProductMedia(
  $productId: String!
  $metadata: String
  $file: Upload!
) {
  productMediaUpload(productId: $productId, metadata: $metadata, file: $file) {
    ...ProductDetails
  }
}
